# Update Log - 2026-01-26 04:05

## 🚀 Features & Enhancements

### 1. Team Management Overhaul
- **Enhanced Modal Design**: Widened the "Add/Edit Member" modal (850px) and reorganized fields into a compact grid layout to eliminate vertical scrolling.
- **Password Visibility**: Added an "Eye" toggle icon to the password field for better usability.
- **Granular Permissions**: Implemented visual permission selection with 3-column grid layout.
- **API Integration**: Connected the "Send Invite" button to the backend invitation flow.

### 2. User Invitation & Verification Cycle
- **Invitation Logic**: Admin invites now trigger an email with a unique verification link.
- **Verification Page**: Created new `VerifyInvite` page handling the token verification process.
- **Security**: Added `isVerified` flag (default false for invites) and `verificationCode` logic. Users cannot login until verified.
- **Mandatory Password Change**: Implemented `mustChangePassword` flag. Users are prompted (via API flag) to change credentials on first login.

### 3. UI/UX Improvements
- **Custom Alert System**: Replaced browser native alerts (`window.alert`, `window.confirm`) with a custom, dark-themed `CustomAlert` component matching the application aesthetics.
- **Login Page**: Added "Show Password" toggle to the main login screen.

## 🛠 Backend Changes
- **Schema**: Updated `User` model with `permissions`, `mustChangePassword`, `verificationCode`.
- **API Endpoints**: 
    - `POST /api/auth/verify-invite`: New endpoint for processing invitation tokens.
    - Updated `inviteUser` to generate tokens and send emails.
    - Updated `authController` to expose permission flags.
